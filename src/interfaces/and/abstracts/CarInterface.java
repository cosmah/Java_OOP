package interfaces.and.abstracts;

public interface CarInterface {
    //define abstract methods(they hide functionality of the methods)
    //abstract methods are public inside an interface

    void start();
    void move(int speed);
}
